let arr = [23, 34, 60, 70, 90, 34, 12, 20];
    function findNumber(i) {
        if (arr = arr.includes(i)) {
            document.write(i + " is found in array");
        }
        else {
            document.write(i + " is not found in array");
        }
    }
findNumber(30);
